#include<iostream>
#include<vector>
using namespace std;

int majEle(vector<int> nums) {
    int n = nums.size();
    int ans = 0,freq = 0;
    for(int i=0;i<n;i++) {
        if(nums[i]==freq) {
            
        }
        for(int val : nums) {

        }
        if(freq>n/2) {
            return ele;
        }
    }
    return 0;
}

int main() {
    vector<int> nums = {1,2,1};
    cout << majEle(nums);
    return 0;
}