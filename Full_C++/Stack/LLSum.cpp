#include <iostream>
#include <vector>
#include <list>
using namespace std;

list<int> splitAtMiddle(list<int>& ll) {
  list
}

int twoSum(list<int>& ll) {
  splitAtMiddle(ll);
}

int main() {
  list<int> ll;
  ll.push_back(1);
  ll.push_back(2);
  ll.push_back(3);
  ll.push_back(4);

  while(!ll.empty()) {
    cout << ll.front() <<  " ";
    ll.pop_front();
  }
  return 0;
}